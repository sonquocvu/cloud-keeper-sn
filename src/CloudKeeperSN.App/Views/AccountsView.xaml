<UserControl x:Class="CloudKeeperSN.App.Views.AccountsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:CloudKeeperSN.App.Controls">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="{StaticResource PagePadding}" MaxWidth="1040" HorizontalAlignment="Stretch">
            <controls:InlineNotification Title="Kết nối an toàn trong chế độ trình diễn" Message="{Binding PageMessage}" Margin="0,0,0,20"/>
            <controls:ProviderAccountCard DataContext="{Binding GoogleDrive}"/>
            <controls:ProviderAccountCard DataContext="{Binding OneDrive}"/>
            <TextBlock Text="CloudKeeperSN không thu thập hoặc lưu mật khẩu Google hay Microsoft. Khi tích hợp thật được bổ sung, cửa sổ đăng nhập hệ thống sẽ được sử dụng." Style="{StaticResource SupportingText}" Margin="4,4,4,0"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
