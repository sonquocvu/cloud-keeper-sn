<Window x:Class="CloudKeeperSN.App.Views.Dialogs.ConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:automation="clr-namespace:System.Windows.Automation;assembly=PresentationCore"
        Title="Xác nhận" SizeToContent="Height" Width="520" MinHeight="240"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="{DynamicResource BackgroundPrimary}">
    <Border Margin="20" Style="{StaticResource ElevatedCard}" Padding="24">
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
            <TextBlock x:Name="TitleText" Style="{StaticResource SectionTitleText}" automation:AutomationProperties.HeadingLevel="Level1" />
            <TextBlock x:Name="MessageText" Grid.Row="1" Style="{StaticResource BodyText}" Margin="0,12,0,0" TextWrapping="Wrap" />
            <TextBlock x:Name="SupportingText" Grid.Row="2" Style="{StaticResource SupportingText}" Margin="0,10,0,0" TextWrapping="Wrap" />
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,24,0,0">
                <Button x:Name="CancelButton" Click="CancelClick" Margin="0,0,8,0" IsCancel="True" />
                <Button x:Name="ConfirmButton" Click="ConfirmClick" IsDefault="True" Style="{StaticResource PrimaryButton}" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
